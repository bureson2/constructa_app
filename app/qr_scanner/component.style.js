import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    camera: {
        flex: 1,
    },
    qrMarker: {
        borderColor: 'white',
        borderRadius: 10,
    },
    hidden: {
        height: 0,
        width: 0,
    },
});

export default styles;